type User = {
  name: string;
  age: number;
  isOnline: boolean;
  lastSeen: null | string;
};

let user: User = {
  name: "Pratick",
  age: 24,
  isOnline: false,
  lastSeen: null,
};

console.log(user);
