function specialNumber(first: number, second: number): void {
  console.log(first, second);
}

let infinityNumber: number = Infinity;
let notANumber: number = NaN;

specialNumber(infinityNumber, notANumber);
